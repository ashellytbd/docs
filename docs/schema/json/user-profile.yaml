"$id": https://example.com/person.schema.json
"$schema": https://json-schema.org/draft/2020-12/schema
title: User profile
type: object
properties:
  id:
    description: User profile id
    type: string
    format: uuid
  public:
    $ref: 'public-user-profile.yaml'
  private:
    $ref: 'private-user-profile.yaml'
required:
  - id
